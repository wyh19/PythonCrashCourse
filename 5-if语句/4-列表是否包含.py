# 使用 xx in xx_list 检查某个值是否存在列表中
requested_toppings = ['mushrooms', 'onions', 'pineapple'] 
print('mushrooms' in requested_toppings) # 存在 ，打印True

# 不存在，使用not in
print('adadads' not in requested_toppings) # 不存在，打印True